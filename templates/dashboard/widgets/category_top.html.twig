<div class="space-y-3">
    {# En-tête avec filtre #}
    <div class="flex items-center justify-between">
        <h4 class="text-sm font-medium text-white">🏆 Meilleurs produits</h4>
        <button 
            data-action="click->widget#refresh"
            class="text-gray-600 hover:text-gray-400 transition text-xs"
            title="Actualiser">
            🔄
        </button>
    </div>

    {# Podium top 3 #}
    <div class="grid grid-cols-3 gap-2 mb-3">
        {# 1er #}
        <div class="text-center p-3 rounded-lg bg-gradient-to-br from-yellow-500/10 to-yellow-600/5 border border-yellow-500/20">
            <div class="text-2xl mb-1">🥇</div>
            <div class="text-xs font-bold text-yellow-400">1er</div>
        </div>
        {# 2ème #}
        <div class="text-center p-3 rounded-lg bg-gradient-to-br from-gray-400/10 to-gray-500/5 border border-gray-400/20">
            <div class="text-2xl mb-1">🥈</div>
            <div class="text-xs font-bold text-gray-400">2ème</div>
        </div>
        {# 3ème #}
        <div class="text-center p-3 rounded-lg bg-gradient-to-br from-orange-600/10 to-orange-700/5 border border-orange-600/20">
            <div class="text-2xl mb-1">🥉</div>
            <div class="text-xs font-bold text-orange-400">3ème</div>
        </div>
    </div>

    {# Liste complète #}
    <div data-widget-target="ranking" class="space-y-2">
        {# Chargé dynamiquement #}
    </div>
</div>
