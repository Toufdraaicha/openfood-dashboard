<div class="space-y-3">
    {# Barre de recherche interactive #}
    <div class="relative">
        <input 
            type="text" 
            placeholder="Rechercher un produit..."
            data-widget-target="searchInput"
            data-action="keyup->widget#search"
            class="w-full px-4 py-2 pl-10 rounded-lg bg-gray-800 border border-gray-700 text-white text-sm 
                   placeholder-gray-500 focus:outline-none focus:border-blue-500 transition"
        >
        <span class="absolute left-3 top-2.5 text-gray-500">🔍</span>
    </div>

    {# Liste des résultats #}
    <div data-widget-target="results" class="space-y-2">
        {# Chargé dynamiquement via JS #}
    </div>

    {# État vide #}
    <div data-widget-target="empty" class="hidden text-center py-8 text-gray-600 text-sm">
        <div class="text-3xl mb-2">📦</div>
        <p>Aucun produit trouvé</p>
    </div>
</div>
