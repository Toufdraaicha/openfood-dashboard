{
    frankenphp
    order php_server before file_server
}

:80 {
    root * /app/public

    # Compression
    encode gzip

    # Symfony front controller
    try_files {path} {path}/ /index.php?{query}

    php_server
}
